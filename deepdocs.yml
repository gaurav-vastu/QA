project_name: Demo Project
language: php

# Where docs live
docs_path: docs/
readme_path: README.md

# Only run when you trigger it
update_mode: manual
update_scope: [readme, api]

# Scan PHP files
include:
  - "**/*.php"
